services:
  ollama:
    volumes:
      - ollama:/root/.ollama
    container_name: ollama
    tty: true
    restart: unless-stopped
    image: ollama/ollama:${OLLAMA_DOCKER_TAG-latest}

  open-webui-localbuild:
    build:
      context: .
      args:
        http_proxy: ${http_proxy}
        https_proxy: ${https_proxy}
        no_proxy: ${no_proxy}
      dockerfile: Dockerfile
    image: ghcr.io/open-webui/open-webui:${WEBUI_DOCKER_TAG-main}
    container_name: open-webui-localbuild
    volumes:
      - open-webui-localbuild:/app/backend/data
      - ./logs/:/root/.npm/_logs/
    depends_on:
      - ollama
    ports:
      - ${OPEN_WEBUI_PORT-3000}:8080
    environment:
      - 'OLLAMA_BASE_URL=http://ollama:11434'
      - 'WEBUI_SECRET_KEY='
      - 'http_proxy=${http_proxy}'
      - 'https_proxy=${https_proxy}'
      - 'no_proxy=${no_proxy}'
      - 'HF_ENDPOINT=${HF_ENDPOINT}'
      - 'WEBUI_SECRET_KEY='
      - 'MICROSOFT_CLIENT_ID=${MICROSOFT_CLIENT_ID}'
      - 'MICROSOFT_CLIENT_SECRET=${MICROSOFT_CLIENT_SECRET}'
      - 'MICROSOFT_CLIENT_TENANT_ID=${MICROSOFT_CLIENT_TENANT_ID}'
      - 'MICROSOFT_REDIRECT_URI=${MICROSOFT_REDIRECT_URI}'
      - 'ENABLE_OAUTH_SIGNUP=${ENABLE_OAUTH_SIGNUP-False}'
      - 'MICROSOFT_OAUTH_SCOPE=${MICROSOFT_OAUTH_SCOPE-"openid email profile offline_access"}'
    extra_hosts:
      - host.docker.internal:host-gateway
    restart: unless-stopped

volumes:
  ollama: {}
  open-webui-localbuild: {}
